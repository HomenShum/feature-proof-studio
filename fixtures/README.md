# fixtures/

Input files referenced by the bundled example spec (`walkthrough.specs.mjs`):

- `cap_table.png` — a synthetic portfolio/cap-table image (generated by
  `make_cap_table.py`, needs Python + Pillow) — uploaded in the "Card → Rows" step.
- `memo.md` — a short investment memo — ingested in the "Document Brain" step.

Replace these with your own fixtures (or remove the steps that use them) when you
adapt `walkthrough.specs.mjs` to your app.
